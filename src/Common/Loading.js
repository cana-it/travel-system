import React from "react";

const Loading = () => {
    return (
        <div className="hfull- w-full flex items-center justify-center text-center transition-[hidden] duration-500 ease-in-out py-5">Loading...</div>
    );
};

export default Loading;